.tribe-editor__rsvp-inline-edit-button {
	align-items: center;
	background: none;
	border: none;
	cursor: pointer;
	display: inline-flex;
	gap: var(--tec-spacer-1);
	padding: 0;
}

.tribe-editor__rsvp-inline-edit-button__icon {
	color: var(--tec-color-text-secondary);
}
