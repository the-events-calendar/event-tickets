/**
 * @typedef {Object} RestLocalizedData
 * @property {string} baseUrl The base URL to the REST API endpoints.
 * @property {string} nonce   The REST API nonce.
 */

/**
 * @type {RestLocalizedData}
 */
export const localizedData = window?.tec?.tickets?.orderModifiers?.rest;
